# XMonadスターターキット

~~~
# Ubuntu
$ sudo apt-get install xmonad dzen2 conky trayer gnome-do
$ sudo apt-get install git-core
$ cd
$ git clone git://github.com/tatsuya6502/xmonad-starter-kit.git .xmonad
~~~
